---
import SectionCard from "./SectionCard.astro";

const { sections } = Astro.props;
---

<div class="flex flex-col gap-6">
    {
        sections.map((section) => (
            <SectionCard entry={section} />
        ))
    }
</div>